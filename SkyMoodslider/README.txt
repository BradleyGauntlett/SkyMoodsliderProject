To get the full functionality to work properly host on a webserver (WAMP for Windows)
and point your browser to:
 
http://localhost/SkyMoodslider/index.html

Or, open in an IDE that has a built in web server (I use Brackets) and
run the preview from there.